//Address if 1d array = basse add + (index - lower index)*Bytes of variable type
// 2d array(row major) = base+{(row - low.row)*no of row + (col. - low.col)}*Bytes of variable type
// 2d array(col major) = base+{(j - low.col)*no of row + (i - low.row)}*Bytes of variable type
// total element= i*j
package introTo2DArray;

public class AddressingOfKArray {
    
}
